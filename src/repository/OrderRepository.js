export default class OrderRepository {
    static searchBy(address) {
        console.log(address);
        const dictionary = {
            sample1 : { id: 100 },
            sample2 : { id: 200 }
        }
        
        return dictionary;
    }
}
